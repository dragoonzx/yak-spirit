describe('Yak Spirit Swap', () => {
  beforeEach(() => cy.visit('/'));

  it('changes tokenIn amount & gets new offers', () => {});

  it('changes tokenIn token & gets new offers', () => {});

  it('changes slippage tolerance & gets new min. to receive', () => {});

  it('changes liquidity sources list & gets exchanges without blacklisted ones', () => {});
});
